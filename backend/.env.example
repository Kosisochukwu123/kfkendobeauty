# ── Server ──
PORT=5000
NODE_ENV=development

# ── Client URL (CORS) ──
CLIENT_URL=http://localhost:5173

# ── MongoDB ──
# MONGO_URI=mongodb://localhost:27017/salonname
# MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/salonname

# ── JWT ──
# JWT_SECRET=your_super_secret_key_here
# JWT_EXPIRE=7d
